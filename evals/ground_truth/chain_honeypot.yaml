server_command: ["uv", "run", "python", "tests/chain_honeypot_server.py"]
tools:
  project_manager:
    expected_findings:
      - category: "data_exfiltration"
        severity: "critical"
        should_find: true
        description: "Multi-step path traversal via project discovery"
      - category: "information_disclosure"
        severity: "high"
        should_find: true
        description: "Internal path disclosure in project details"
  user_directory:
    expected_findings:
      - category: "authentication"
        severity: "critical"
        should_find: true
        description: "Admin token exfiltration via user search chain"
      - category: "information_disclosure"
        severity: "medium"
        should_find: true
        description: "Role and token prefix disclosure in profiles"
